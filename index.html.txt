<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HEIC to JPG Dönüştürücü - Ücretsiz & Hızlı (iPhone Fotoğraf Açma)</title>
    <meta name="description" content="iPhone HEIC ve HEIF fotoğraflarınızı anında JPG veya PNG formatına dönüştürün. Yükleme yok, %100 güvenli ve tarayıcı üzerinde tamamen ücretsiz.">
    <meta name="keywords" content="heic to jpg, heic dönüştürücü, iphone fotoğraf açma, heif to png, ücretsiz heic converter">
    
    <!-- Open Graph / Sosyal Medya -->
    <meta property="og:title" content="HEIC to JPG Dönüştürücü - Ücretsiz & Hızlı">
    <meta property="og:description" content="iPhone fotoğraflarınızı kaliteden ödün vermeden tarayıcınızda ücretsiz JPG'ye dönüştürün.">
    <meta property="og:type" content="website">

    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- heic2any Kütüphanesi -->
    <script src="https://unpkg.com/heic2any@0.0.4/dist/heic2any.min.js"></script>
</head>
<body class="bg-slate-50 text-slate-800 min-h-screen flex flex-col justify-between font-sans">

    <!-- Header -->
    <header class="bg-white border-b border-slate-200 py-4 px-6 text-center shadow-sm">
        <h1 class="text-2xl font-bold text-indigo-600">HEIC2JPG Quick</h1>
        <p class="text-xs text-slate-500 mt-1">iPhone Fotoğraf Formatı Dönüştürücü (%100 Güvenli & Yerel)</p>
    </header>

    <!-- Main Content -->
    <main class="max-w-3xl mx-auto px-4 py-8 flex-grow w-full">
        <!-- Hero Section -->
        <div class="text-center mb-8">
            <h2 class="text-3xl font-extrabold text-slate-900 mb-2">HEIC Fotoğraflarınızı Anında JPG'ye Çevirin</h2>
            <p class="text-slate-600 text-sm">Dosyalarınız sunucuya yüklenmez, tüm işlem tamamen bilgisayarınızda gerçekleşir.</p>
        </div>

        <!-- Upload & Convert Card -->
        <div class="bg-white p-6 rounded-2xl shadow-md border border-slate-200">
            <!-- Drop Area -->
            <div id="dropZone" class="border-2 border-dashed border-indigo-300 rounded-xl p-8 text-center cursor-pointer hover:border-indigo-500 hover:bg-indigo-50/50 transition">
                <input type="file" id="fileInput" accept=".heic, .HEIC, .heif, .HEIF" class="hidden">
                <div class="flex flex-col items-center gap-2">
                    <svg class="w-12 h-12 text-indigo-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                    <span class="font-semibold text-slate-700">HEIC Dosyanızı Buraya Sürükleyin</span>
                    <span class="text-xs text-slate-400">veya bilgisayarınızdan seçmek için tıklayın</span>
                </div>
            </div>

            <!-- Controls (Hidden initially) -->
            <div id="controls" class="mt-6 hidden flex-col gap-4">
                <div class="flex items-center justify-between bg-slate-100 p-3 rounded-lg text-sm">
                    <span id="fileName" class="font-medium truncate max-w-[200px]">dosya.heic</span>
                    <div class="flex items-center gap-2">
                        <label class="text-xs text-slate-600">Format:</label>
                        <select id="formatSelect" class="bg-white border border-slate-300 rounded px-2 py-1 text-xs">
                            <option value="image/jpeg">JPG</option>
                            <option value="image/png">PNG</option>
                        </select>
                    </div>
                </div>

                <button id="convertBtn" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-medium py-2.5 rounded-lg transition shadow-sm">
                    Dönüştür ve İndir
                </button>
            </div>

            <!-- Status / Progress -->
            <div id="status" class="mt-4 text-center text-sm font-medium text-slate-600 hidden"></div>

            <!-- Preview & Download Area -->
            <div id="resultArea" class="mt-6 hidden text-center">
                <img id="previewImg" class="max-h-64 mx-auto rounded-lg shadow border mb-4" src="" alt="Dönüştürülen Görsel">
                <a id="downloadBtn" href="" download="image.jpg" class="inline-block bg-emerald-600 hover:bg-emerald-700 text-white font-medium px-6 py-2 rounded-lg transition">
                    İndir
                </a>
            </div>
        </div>

        <!-- SEO Text Content -->
        <article class="mt-12 bg-white p-6 rounded-2xl border border-slate-200 text-sm text-slate-600 space-y-4">
            <h3 class="text-base font-bold text-slate-800">HEIC Formatı Nedir? Neden JPG'ye Dönüştürülmeli?</h3>
            <p>HEIC (High Efficiency Image Format), Apple'ın iOS 11 ile birlikte iPhone cihazlarda varsayılan olarak kullanmaya başladığı yüksek verimlilik sunan bir görsel formatıdır. Fotoğraf kalitesini bozmadan daha az yer kaplar ancak Windows, Android veya bazı web siteleri tarafından doğrudan açılamayabilir.</p>
            <h3 class="text-base font-bold text-slate-800">Gizliliğiniz Nasıl Korunur?</h3>
            <p>Sitemiz dosyalarınızı hiçbir uzak sunucuya (server) göndermez. Tüm dönüştürme işlemi tarayıcınızın kendi işlem gücü kullanılarak bilgisayarınızda yapılır. Bu sayede fotoğraflarınız tamamen gizli kalır.</p>
        </article>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-slate-200 py-4 text-center text-xs text-slate-400">
        &copy; 2026 HEIC2JPG Quick — Tüm İşlemler İstemci Taraflıdır (Client-Side).
    </footer>

    <!-- Script Logic -->
    <script>
        const dropZone = document.getElementById('dropZone');
        const fileInput = document.getElementById('fileInput');
        const controls = document.getElementById('controls');
        const fileName = document.getElementById('fileName');
        const convertBtn = document.getElementById('convertBtn');
        const status = document.getElementById('status');
        const resultArea = document.getElementById('resultArea');
        const previewImg = document.getElementById('previewImg');
        const downloadBtn = document.getElementById('downloadBtn');
        const formatSelect = document.getElementById('formatSelect');

        let currentFile = null;

        dropZone.addEventListener('click', () => fileInput.click());

        dropZone.addEventListener('dragover', (e) => {
            e.preventDefault();
            dropZone.classList.add('border-indigo-500', 'bg-indigo-50/50');
        });

        dropZone.addEventListener('dragleave', () => {
            dropZone.classList.remove('border-indigo-500', 'bg-indigo-50/50');
        });

        dropZone.addEventListener('drop', (e) => {
            e.preventDefault();
            dropZone.classList.remove('border-indigo-500', 'bg-indigo-50/50');
            if (e.dataTransfer.files.length) {
                handleFile(e.dataTransfer.files[0]);
            }
        });

        fileInput.addEventListener('change', (e) => {
            if (e.target.files.length) {
                handleFile(e.target.files[0]);
            }
        });

        function handleFile(file) {
            currentFile = file;
            fileName.textContent = file.name;
            controls.classList.remove('hidden');
            controls.classList.add('flex');
            resultArea.classList.add('hidden');
            status.classList.add('hidden');
        }

        convertBtn.addEventListener('click', async () => {
            if (!currentFile) return;

            status.textContent = 'Dönüştürülüyor, lütfen bekleyin...';
            status.classList.remove('hidden');
            convertBtn.disabled = true;

            try {
                const targetFormat = formatSelect.value;
                const blob = await heic2any({
                    blob: currentFile,
                    toType: targetFormat,
                    quality: 0.9
                });

                const url = URL.createObjectURL(blob);
                previewImg.src = url;
                downloadBtn.href = url;
                
                const ext = targetFormat === 'image/png' ? 'png' : 'jpg';
                downloadBtn.download = currentFile.name.replace(/\.[^/.]+$/, "") + "." + ext;

                status.textContent = 'Dönüştürme Başarılı!';
                resultArea.classList.remove('hidden');
            } catch (err) {
                status.textContent = 'Hata oluştu! Lütfen geçerli bir .heic veya .heif dosyası seçin.';
                console.error(err);
            } finally {
                convertBtn.disabled = false;
            }
        });
    </script>
</body>
</html>